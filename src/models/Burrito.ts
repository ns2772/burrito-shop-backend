class Burrito {
  name: string;
  size: string;
  price: number;

  constructor(name: string, size: string, price: number) {
      this.name = name;
      this.size = size;
      this.price = price;
  }
}

export default Burrito;
